###############################################
# ForgeX Bot - Environment Variables #
###############################################

# Copy this file to `.env` and fill in your real values.
# NEVER commit your real `.env` file.

# MT5 Credentials
MT5_LOGIN=12345678
MT5_PASSWORD=your_mt5_password
MT5_SERVER=YOUR-MT5-SERVER

# (Optional) MT5 Terminal path if auto-detect fails
# MT5_PATH=C:\\Program Files\\MetaTrader 5\\terminal64.exe

# Telegram Bot
TELEGRAM_BOT_TOKEN=1234567890:ABCDEF-your-bot-token
TELEGRAM_CHAT_ID=-1001234567890

# Override logging level (optional)
# LOG_LEVEL=INFO

###############################################
# Notes:
# - All variables are optional; if unset, values from config/config.yaml are used.
# - Prefer keeping secrets ONLY in .env (remove them from config.yaml).
# - After editing .env you can run: python main.py --test
###############################################
